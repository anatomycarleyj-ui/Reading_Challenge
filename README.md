# Reading Challenge — Streamlit wrapper

This app wraps the existing Reading Challenge HTML in a Streamlit app so it can be embedded in Notion using Notion's supported Streamlit embed flow.

Entrypoint: `streamlit_app.py`

No book data or styling has been rewritten; the original HTML is embedded intact inside the Python file.
